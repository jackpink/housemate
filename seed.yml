before_compile:
  - echo "Before compile"
  - n 20.9.0
  - npm install @node-rs/argon2-linux-arm64-gnu

#compile:
#  - echo "Override compile"

before_build:
  - echo "Before build"
  - npx sst version

before_deploy:
  - echo "Before deploy"

after_deploy:
  - echo "After deploy"

before_remove:
  - echo "Before deploy"

after_remove:
  - echo "After deploy"
